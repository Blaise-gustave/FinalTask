<%= form_for ([@post, @post.comments.build]) do |f| %>

<%= f.text_area :content, class:"single-input", placeholder:"enter comment", required:true%>
<%= f.submit "post comment", class:"genric-btn info-border circle" %>
<%end%>
